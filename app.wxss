/**app.wxss**/

page {
  height: 100%;
}

.container {
  height: 100%;
  display: flex;
  flex-direction: column;
  /* align-items: center;
  justify-content: space-between; */
  box-sizing: border-box;
}

/* button 默认 */

button {
  padding: 0;
  text-align: left;
}

button::after {
  border: none;
}